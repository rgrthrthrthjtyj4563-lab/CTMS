# DEVELOPMENT_PLAN

## 目标

本计划用于把当前项目从“本地草稿堆”整理成“清晰、可回退、可验收、可继续开发的 APP V1 基线”。

项目短期目标：

> 打通 CRA 的 IMV 主流程，并建立稳定的协作和验收秩序。

项目中期目标：

> 在主流程稳定后，实现低噪音、统一规则的时间提醒 M0-M1。

项目长期目标：

> 在稳定 APP V1 上继续扩展语音、PM 审核、Issue 闭环、工时与报告自动化。

## 总体路线

| 阶段 | 名称 | 目标 |
|---|---|---|
| Phase 0 | 项目收口与基线建立 | 固定当前 APP V1 状态，建立 AI 协作秩序 |
| Phase 1 | IMV 主流程收口 | 打通 CRA 完整工作流 |
| Phase 1.5 | 测试与质量收口 | 让项目具备可交付基础 |
| Phase 2-M0 | 时间提醒统一规则 | 统一 dueStatus / dueLabel / sortPriority |
| Phase 2-M1 | 时间提醒页面落地 | 工作台、现场页、总结页、待办、Issue、行动包统一展示 |
| Phase 3 | V1 增强 | 语音、PM 审核、报告、体验优化 |

## Phase 0：项目收口与基线建立

### 目标

先建立秩序，不写新功能。

### 任务

1. 创建 `PROJECT_STATE.md`
2. 创建 `AI_TASKS.md`
3. 创建 `DEVELOPMENT_PLAN.md`
4. 建议创建 `wip/app-v1-rebaseline` 分支
5. 清理不能入库的本地运行产物
6. 暂停时间提醒和新功能开发

### 完成标准

- 根目录存在项目状态文件
- 根目录存在 AI 分工文件
- 根目录存在开发计划文件
- 所有 AI 有统一阅读入口
- 当前 APP V1 被明确为当前产品基线

## Git 分支规划

### 推荐分支

| 分支 | 用途 |
|---|---|
| `main` | 稳定交付版本 |
| `develop` | 日常集成分支 |
| `wip/app-v1-rebaseline` | 当前 APP V1 草稿基线 |
| `fix/main-flow-imv` | 修 IMV 主流程 |
| `fix/api-test-prisma` | 修 API 自动测试 |
| `feature/time-reminder-m0` | 时间提醒统一规则 |
| `feature/time-reminder-m1` | 时间提醒页面落地 |
| `feature/pm-review-flow` | PM 审核增强 |
| `feature/voice-input-hardening` | 语音输入增强 |

### 当前建议

当前最应该建立：

```text
wip/app-v1-rebaseline
```

含义：

> 当前电脑里的 APP V1 开发状态，从这里开始作为统一协作基线，但它还不是正式交付版。

### main 分支规则

暂时不要把当前 WIP 直接推到 `main`。

进入 `main` 前必须满足：

1. API test 通过
2. 主流程人工验收通过
3. Cursor / Codex 分工文件存在
4. 当前大规模删除旧代码的意图已经确认
5. 时间提醒不再和主流程修复互相冲突

## Phase 1：IMV 主流程收口

### 目标

不是页面“看起来有”，而是 CRA 能完整走完一次 IMV。

### 主故事验收

| 顺序 | 验收项 | 通过标准 |
|---|---|---|
| 1 | 登录 | CRA 能用测试账号登录 |
| 2 | 工作台 | 能看到今日 IMV |
| 3 | 开始访视 | 点击后进入进行中访视 |
| 4 | 现场记录 | 能输入文字、上传附件或语音 |
| 5 | 结束访视 | 2 秒内进入总结页，不能卡住 |
| 6 | 总结页 | 能看到访视摘要、问题、工时 |
| 7 | 行动包 | 能确认、编辑、删除建议项 |
| 8 | 提交 | CRA 能提交给 PM |
| 9 | PM 审核 | PM 能批准或退回 |
| 10 | 待办跳转 | 待办能进入对应详情 |

### 最高优先级问题

1. 结束访视不能卡住，必须进入总结页
2. 行动包必须能确认并提交
3. 待办点击必须有明确结果
4. PM 审核必须能完成或退回

### Phase 1 禁止事项

本阶段不做：

- 秒级倒计时
- 大面积时间提醒 UI
- 患者 ePRO
- SubjectVisit
- 完整 CTMS 日历
- 新角色扩展
- 大规模 UI 重构

## Phase 1.5：测试与质量收口

### 目标

让项目具备进入正式开发协作的最低质量门槛。

### 必须通过的检查

| 检查 | 目标 |
|---|---|
| lint | 静态检查通过 |
| typecheck | 类型检查通过 |
| build | APP / API 能构建 |
| mobile smoke | 移动端主流程脚本通过 |
| API test | API 自动测试通过 |
| 人工验收 | IMV 主流程人工点通 |

### 当前已知情况

| 检查 | 状态 |
|---|---|
| lint | 已通过过一次 |
| typecheck | 已通过过一次 |
| build | 已通过过一次 |
| mobile smoke | 已通过过一次 |
| API test | 仍需修复 |
| 人工验收 | 仍需完整执行 |

### API 测试修复任务

单独开任务：

```text
fix/api-test-prisma
```

目标：

1. 确认 Node 版本
2. 确认 Prisma 版本
3. 确认 SQLite 测试库创建方式
4. 让 API 自动测试完整通过

这个任务由 Codex 负责更合适，不要让 Cursor 在修 APP 页面时同时处理数据库和测试环境。

## Phase 2：倒计时与时间提醒优化

### 产品定义

本轮不做“秒级倒计时”或强压迫式计时器。

本轮做的是：

> 让 CRA 明确现在该做什么、什么快到期、什么已逾期，同时保持 Agent-first、低打扰、现场记录优先的产品定位。

### 覆盖三个场景

1. 访视前提醒
2. 访视中低噪音提示
3. 访视后行动项 / Issue 截止管理

## Phase 2-M0：统一时间状态能力

### 目标

先统一规则，再改页面。

不允许每个页面自己写一套“临近 / 逾期”判断。

### 统一时间状态

| 状态 | 含义 | 展示文案 | 排序优先级 |
|---|---|---|---|
| `OVERDUE` | 已超过截止日期 | 已逾期 N 天 | 1 |
| `DUE_TODAY` | 今天到期 | 今日到期 | 2 |
| `DUE_SOON` | 未来 1-3 天到期 | 截止临近 | 3 |
| `VISIT_TODAY` | 今日计划访视 | 今日 IMV | 4 |
| `NORMAL` | 普通待办 | 普通 | 5 |
| `NO_DUE_DATE` | 无截止时间 | 待补充 | 6 |

### 统一字段

建议统一输出：

```text
dueStatus
dueLabel
sortPriority
dueDate
overdueDays
```

前端只负责展示，不在多个页面各自计算。

### 统一排序

全 App 统一：

1. 已逾期
2. 今日到期
3. 截止临近
4. 今日访视
5. 普通待办

## Phase 2-M1：页面落地

### 工作台

实现目标：

- 今日 IMV 显示计划开始时间
- 显示距开始时间
- 显示是否可开始
- Issue / 待办显示今日到期、截止临近、已逾期 N 天
- 排序按统一优先级

验收标准：

| 验收项 | 标准 |
|---|---|
| 今日 IMV | 显示计划时间和可开始状态 |
| 待办排序 | 已逾期排最前 |
| 无今日访视 | 不出现可误点的开始按钮 |

### IMV 现场页

实现目标：

- 保留开始时间
- 保留已进行时长 / 总时长
- 只显示分钟级时间
- 不出现秒级跳动大秒表
- 临近计划结束时，只提示关键未完成事项
- 不自动结束访视

允许提醒：

- 未完成关键活动
- 关键证据缺失
- 严重 Issue 未记录
- 计划结束临近

禁止提醒：

- 普通缺失项频繁弹窗
- 红色大面积警告
- 自动结束访视

### 访视总结页

实现目标：

- 展示实际开始时间
- 展示实际结束时间
- 展示总时长
- 展示工时候选
- 结束时间与计划时间偏差明显时提示 CRA 确认
- 行动项 / Issue / 证据补充必须带责任人和截止时间
- 缺失时进入待补充问题

### 待办页

实现目标：

- 使用统一 `dueStatus`
- 使用统一 `dueLabel`
- 使用统一颜色
- 使用统一排序
- 点击必须跳转到对应对象

### Issue 页

实现目标：

- 同一个 Issue 在工作台、待办页、Issue 详情页状态一致
- 已逾期 / 今日到期 / 截止临近颜色一致
- 截止时间缺失时有补充入口

### 行动包

实现目标：

- 行动项如果需要后续处理，必须有责任人和截止时间
- 缺失时不能静默提交
- 缺失项进入“待补充问题”
- 行动包生成的 Issue / Todo 使用统一时间状态

## Phase 2 禁止范围

本轮不做：

- 患者随访倒计时
- ePRO 倒计时
- SubjectVisit 窗口期管理
- 传统 CTMS 日历
- 自动结束访视
- 秒级大倒计时
- 高频弹窗提醒
- 每个页面各自写时间判断

## Phase 2 测试计划

### 工作台

| 测试项 | 预期 |
|---|---|
| 今日 IMV | 显示计划时间和可开始状态 |
| 临近截止 | 显示“截止临近” |
| 今日到期 | 显示“今日到期” |
| 已逾期 | 显示“已逾期 N 天” |
| 排序 | 已逾期排最前 |
| 无今日访视 | 不显示可误点开始按钮 |

### IMV 现场页

| 测试项 | 预期 |
|---|---|
| 开始时间 | 正确显示 |
| 已进行时长 | 分钟级显示 |
| 秒级倒计时 | 不出现 |
| 计划结束到达 | 只提醒，不自动结束 |
| 结束访视 | 进入总结页 |

### 访视总结页

| 测试项 | 预期 |
|---|---|
| 实际开始 | 正确显示 |
| 实际结束 | 正确显示 |
| 总时长 | 与开始结束一致 |
| 工时候选 | 与时长同步 |
| 修改结束时间 | 工时候选同步更新 |
| 缺失截止时间 | 有补充入口 |

### 待办与 Issue

| 测试项 | 预期 |
|---|---|
| 同一 Issue 多页面 | 时间状态一致 |
| 颜色 | 一致 |
| 文案 | 一致 |
| 点击待办 | 跳转到对应 Issue、行动包或访视 |
| 无跳转目标 | 显示明确提示，不静默失败 |

## 推荐执行时间表

### 第 1 天：项目收口

目标：建立秩序，不写新功能。

任务：

1. 创建项目状态文件
2. 创建 AI 分工文件
3. 创建开发计划文件
4. 建立 `wip/app-v1-rebaseline`
5. 清理本地运行产物
6. 提交当前 APP V1 基线

### 第 2 天：主流程修复

目标：先让 IMV 主流程能走通。

任务：

1. Cursor 修 APP 主流程卡点
2. Codex 修 API 测试环境
3. 不做时间提醒
4. 不做新模块

### 第 3 天：主流程验收

目标：人工完整走一遍。

验收：

1. 登录
2. 今日 IMV
3. 开始访视
4. 记录
5. 结束
6. 总结
7. 行动包
8. 提交
9. PM 审核
10. 待办跳转

### 第 4 天：时间提醒 M0

目标：统一规则，不大改页面。

任务：

1. 统一 `dueStatus`
2. 统一 `dueLabel`
3. 统一 `sortPriority`
4. 补测试
5. 小范围适配页面

### 第 5 天：时间提醒 M1

目标：页面展示落地。

任务：

1. 工作台展示
2. 待办展示
3. Issue 展示
4. 总结页时间闭环
5. 现场页低噪音提示

### 第 6 天以后：V1 增强

可选增强：

1. 语音输入稳定性
2. PM 审核体验
3. 报告草稿质量
4. Issue 闭环
5. 工时确认体验
6. 离线草稿同步

## 当前最推荐的下一步

完成 Phase 0 后，下一步不要直接做时间提醒。

下一步应是：

1. 建立 `wip/app-v1-rebaseline`
2. 提交当前 APP V1 WIP 基线
3. 开 `fix/main-flow-imv`
4. 只修主流程阻塞点
5. 主流程验收通过后，再开 `feature/time-reminder-m0`

