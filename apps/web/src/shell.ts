/**
 * Web back-office Phase 0 stub.
 *
 * Per phase plan, Phase 0 only establishes the workspace and domain
 * contracts. The actual Vite/Next.js shell + productized `App.tsx` is built
 * in Phase 1. This module exists so:
 *   1. `tsc --noEmit` succeeds for the workspace.
 *   2. Future phases have a single import path (`./shell`) for the auth
 *      context, RBAC-aware navigation, and audit-aware data layer.
 */
export const WEB_PHASE = "Phase 0 — contracts only; UI shells land in Phase 1.";